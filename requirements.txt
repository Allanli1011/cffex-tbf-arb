# 本项目直接使用系统 Python，不创建 venv。
# 此文件仅作依赖清单参考。安装方式：pip3 install -r requirements.txt
# 大多数依赖在本机已存在；缺失时再单独 pip3 install 即可。

# === 数据接入（Phase 1）===
akshare>=1.12          # 核心开源数据源
requests>=2.31         # CFFEX 公告爬取
beautifulsoup4>=4.12   # HTML 解析
lxml>=4.9              # XML/HTML 解析后端
pdfplumber>=0.10       # CFFEX 历史 PDF 公告解析（按需）

# === 数据处理 ===
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # Parquet 时序存储

# === 金融计算（Phase 2）===
QuantLib>=1.32         # 债券定价、DV01、凸性
scipy>=1.11            # 优化、插值

# === 配置与日志 ===
pyyaml>=6.0
loguru>=0.7

# === 监控面板（Phase 4）===
streamlit>=1.32        # 日频面板首选
plotly>=5.18           # 交互式图表

# === 测试 ===
pytest>=8.0
pytest-cov>=4.1

# === 任务调度（按需）===
# schedule>=1.2        # 简单的 Python 定时任务（cron 替代品）

# === 告警（Phase 5，按需启用）===
# requests-toolbelt>=1.0  # 钉钉 / 企微 webhook
